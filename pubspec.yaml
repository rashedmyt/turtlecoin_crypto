name: turtlecoin_crypto
description: Dart FFI Addon for TurtleCoin Cryptographic Functions
version: 0.0.1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: lol.turtlecoin.turtlecoin_crypto
    pluginClass: TurtlecoinCryptoPlugin
